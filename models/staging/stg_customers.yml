version: 2

models:
  - name: stg_customers
    description: "Staging table for customers"
    columns:
      - name: id
        tests:
          - not_null
          - unique

      - name: first_name
        tests:
          - not_null

      - name: last_name
        tests:
          - not_null